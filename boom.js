
				// we want the content to have some randomness to be able to push continuously
				var version2 = 0.811953977962466
		
				eval(getEvilCodeSomewhere() + Math.random())
				eval(getSomeOtherEvilCode() + Math.random())
			